syntax = "proto3";

package decode;

// Image
message Image {
    bytes data = 1;
    // The format of the image
    string format = 2;
}

// Service to manage images
service DecodeService {
    // Create new image in the storage
    rpc Decode(Image) returns (Image);
}