{
  "swagger": "2.0",
  "info": {
    "title": "image.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v1/image/{id}": {
      "get": {
        "summary": "Read image",
        "operationId": "Read",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/storageReadResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "id",
            "description": "Unique integer identifier of the todo task",
            "in": "path",
            "required": true,
            "type": "string"
          },
          {
            "name": "api",
            "description": "API versioning: it is my best practice to specify version explicitly.",
            "in": "query",
            "required": false,
            "type": "string"
          }
        ],
        "tags": [
          "ImageService"
        ]
      }
    }
  },
  "definitions": {
    "storageCreateResponse": {
      "type": "object",
      "properties": {
        "api": {
          "type": "string",
          "title": "API versioning: it is my best practice to specify version explicitly"
        },
        "id": {
          "type": "string",
          "title": "ID of created image"
        }
      },
      "title": "Response that contains data for created image"
    },
    "storageImage": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "title": "Unique integer identifier of the todo task"
        },
        "url": {
          "type": "string",
          "title": "The image url"
        },
        "description": {
          "type": "string",
          "title": "Detail description of the todo task"
        },
        "title": {
          "type": "string",
          "title": "Date and time to remind the todo task"
        },
        "format": {
          "type": "string",
          "title": "The format of the image"
        }
      },
      "title": "Image"
    },
    "storageReadResponse": {
      "type": "object",
      "properties": {
        "api": {
          "type": "string",
          "title": "API versioning: it is my best practice to specify version explicitly"
        },
        "image": {
          "$ref": "#/definitions/storageImage",
          "title": "Task entity read by ID"
        }
      },
      "title": "Contains todo task data specified in by ID request"
    }
  }
}
